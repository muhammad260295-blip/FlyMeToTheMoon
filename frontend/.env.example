# Optional — production only. Public API base URL (no trailing slash).
# Example: https://your-backend.railway.app
# Leave unset in local dev; Vite proxies /api to the backend.
# Never put SERPAPI_KEY here — it is server-only on the API host.
# VITE_API_BASE_URL=
